#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('ls') {
            steps {
                sh 'ls'
            }
        }
        stage('pwd') {
            steps {
                sh 'pwd'
            }
        }
    }
}